# ML
faster-whisper>=1.1.0
speechbrain>=1.0.0
scipy>=1.10.0
# ВАЖНО: torch и torchaudio устанавливать отдельно с CUDA ПЕРЕД speechbrain:
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
# Если speechbrain перезаписал torch на CPU-версию, переустановите CUDA torch:
# pip install torch torchaudio --force-reinstall --index-url https://download.pytorch.org/whl/cu124

# Аудио
imageio-ffmpeg>=0.5.1

# GUI
customtkinter>=5.2.0

# GPU
pynvml>=11.5.0

# Модели
huggingface-hub>=0.20.0

# AI Analysis: web search (опционально, для поиска ссылок через API)
anthropic>=0.40.0
